<div class="flex justify-between items-center">
  <h1 class="font-bold text-2xl"><%= t("gallery.image.images") %></h1>
</div>

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-1 w-96">
  <%= render partial: "images/image", collection: @images, locals: { image_class: "max-h-24 max-w-24" } %>
</div>
