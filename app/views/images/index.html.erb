<div class="flex justify-between items-center">
  <h1 class="font-bold text-2xl"><%= t("home.images") %></h1>
  <%= link_to t("image.new"), new_event_path,
    class: "text-white bg-green-700 hover:bg-green-800 font-medium rounded-lg px-3 py-2" %>
</div>

<div id="images" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-1 w-96">
  <%= render partial: "images/image", collection: @images, locals: { image_class: "max-h-24 max-w-24" } %>
</div>
