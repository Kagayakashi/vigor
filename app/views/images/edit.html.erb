<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing image</h1>

  <%= render "form", image: @image %>

  <%= link_to "Show this image", image_category_image_path(@image_category, @image), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to images", image_category_images_path(@image_category), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
